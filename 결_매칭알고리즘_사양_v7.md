# 결 (Gyeol) — 매칭 알고리즘 사양 (v7)

> 본 문서는 v6에 코덱스 검토 13개 변경을 반영한 v7이다. 가장 큰 변경은 (1) compatibility_assessment를 *basic*과 *explanation_payload*로 분리, (2) explanation_payload를 *전체 페어가 아닌 최종 큐 후보에만* 산출, (3) stance distance matrix 도입, (4) shared_rejection_targets 추가, (5) boundary_check_payload 신설, (6) post-polish validation 추가, (7) 캐시 key 방향성 분리.

> v5에서 *그대로*라고 참조했던 §2(정규화 레이어), §3(데드라인 필터링), §4(호환 점수 계산), §5(필터 통과 판정), §6(재시작), §7(매칭 풀 자격), §10(시스템 동작 보장)은 본 v7에서도 *v5 참조*로 유지. 다음 사이클에서 v7 standalone 재수록.

> ## 문서 사용 가이드
> 
> 본 v7 단독으로는 standalone 아님. 구현 시작할 때는 다음 문서를 함께 읽어야 함:
> 
> 1. **결_매칭알고리즘_사양_v7.md** (본 문서) — 1·4·8 절의 핵심 변경
> 2. **결_매칭알고리즘_사양_v5.md** — §2, §3, §5, §6, §7, §9, §10 (본문 참조)
> 3. **결_매칭알고리즘_사양_v3.md** — §3 데드라인 필터링 상세 (v5가 v3을 참조)
> 4. **결_매칭알고리즘_사양_v4.md** — §4 호환 점수 계산 상세 (v5가 v4를 참조)
> 5. **결_충돌매트릭스_v2.md** — System Hard / User Hard 충돌 규칙
> 6. **결_AI프롬프트_데이터계약_v7.md** — 입력 데이터 (analyses, normalized_profiles)
> 7. **결_추천이유매트릭스엔진_v3.md** — §8 매트릭스 엔진 호출 시점의 매트릭스 엔진 사양
> 
> 다음 사이클에서 본 v7을 *완전 standalone 문서*로 재수록 예정 (§13.10에 v5/v3/v4 참조 영역 명시).

> **v6 → v7 주요 변경**: §13 참조.

---

## 1. 매칭 시스템 구조

### 1.1 본 앱 정체성 (v6 그대로)

본 앱은 *결혼 또는 매우 진지한 장기 연애를 목표로 하는 진지한 파트너십 매칭 앱*이다.

### 1.2 전체 흐름 (v7 핵심 변경)

```
[모든 매칭 풀 사용자의 데이터]
            ↓
[정규화 레이어] (오프라인)
            ↓
[매칭 풀 진입 자격 검사]
            ↓
[1단계: 전체 페어 — 결정론적 매칭 평가]
  - System Hard / User Hard 필터링
  - 호환 점수 계산
  - alignment_by_domain.alignment_level 산출 (영역별 강도만)
  - compatibility_assessment_basic 완성
            ↓
[2단계: 점수 정렬 + 필터 통과 후보 산출]
            ↓
[3단계: 최종 큐 후보 — 노출되는 후보에만]
  - explanation_payload 산출 (pair_reason_atoms + public sentences)
  - boundary_check_payload 산출 (boundary_check 페어인 경우)
            ↓
[4단계: 매트릭스 엔진 호출]
  - draft_narrative 결정론적 조립
  - 후편집 평가 함수
  - 프롬프트 C 후편집 (LLM, 선택적)
  - post-polish validation
            ↓
[matches 테이블 저장]
            ↓
[목록 노출 → 양방향 [관심 있음] → 즉시 대화방]
```

**v6 → v7 핵심 변경**: pair_reason_atoms와 public sentences가 *전체 페어 호환 점수 계산*에서 분리되어 *최종 큐 후보 산출 시점*으로 이동. 부하 절감 + 본 앱 *분석은 깊게, 매칭은 결정론적* 정체성 강화.

### 1.3 결정론적 vs LLM 분리 (v6 그대로 + 강화)

**결정론적 백엔드**:
- compatibility_assessment_basic 산출 (전체 페어)
- explanation_payload 산출 (최종 큐 후보)
- boundary_check_payload 산출 (boundary_check 페어)
- 매트릭스 엔진 1차 조립
- post-polish validation (v7 신설)

**오프라인 LLM 보조**:
- 신규 자연어 라벨 canonical id 매핑
- 명시 Dealbreaker 정규화 (프롬프트 E)

**온라인 LLM (선택적)**:
- 추천 이유 후편집 (프롬프트 C, Flash-Lite, 페어당 0-1회)

### 1.4 데이터 비공개 원칙 (v6 그대로)

v6 §1.4 그대로 + 다음 추가:
- `compatibility_assessment_basic`: internal_only
- `explanation_payload`: internal_only
- `boundary_check_payload`: internal_only

---

## 2. 정규화 레이어

**v5 §2 + raw quote 차단 강화** (v7 추가).

### 2.1 v5 그대로 영역

canonical_principles, axis_positions, sacred_targets, disgust_targets, dealbreaker_targets, domain_salience 스키마와 영역별 canonical 사전, 정규화 처리 절차, 오프라인 LLM 호출 사양 — 모두 v5 §2 그대로.

### 2.2 raw quote 차단 (v7 신설)

정규화 레이어가 분석 결과를 normalized profile로 변환할 때 다음 검사 수행:

```python
def detect_raw_quote_in_summary(summary):
    """
    summary.where, summary.why, summary.how, summary.tension.text에서
    사용자 raw 답변 직접 인용 감지.
    """
    
    # 검사 1: 따옴표 패턴
    quote_patterns = [
        r"['\"]",
        r"['\"][^'\"]{5,}['\"]",
        r"<<.*?>>",
    ]
    
    # 검사 2: raw answer와의 n-gram overlap
    # 8자 이상 연속 substring 일치
    overlap = check_ngram_overlap(summary, raw_answer, min_length=8)
    
    if any(re.search(p, summary) for p in quote_patterns):
        return True
    if overlap:
        return True
    return False
```

raw quote 감지 시:
- normalized_profile 생성 차단
- operator_review_queue에 추가
- 운영자가 검토 후 summary 재생성 또는 수동 수정

이 차단으로 매트릭스 엔진 입력 시점에는 *raw-safe 보장*. 매트릭스 엔진 자체에는 raw quote 감지 휴리스틱 추가 (다중 방어선).

---

## 3. 데드라인 필터링

**v5 §3 그대로.** System Hard / User Hard 분리 메커니즘 그대로 유지.

---

## 4. 호환 점수 계산 + 데이터 분리 (v7 핵심 변경)

### 4.1 v6 → v7 데이터 분리

v6: 호환 점수 계산이 alignment_summary + pair_reason_atoms + public sentences 모두 산출
v7: *2단계 분리*

**1단계 — 전체 페어 호환 점수 계산** (compatibility_assessment_basic):
- final_score
- qualitative_label
- comparable_domain_count
- comparable_domain_weight_sum
- alignment_by_domain (단, alignment_level + alignment_summary만)
- shared_sacred_targets
- queue_reason

**2단계 — 최종 큐 후보 explanation 확장** (explanation_payload):
- 영역별 pair_reason_atoms
- 영역별 public_alignment_sentence
- 영역별 public_tension_sentence
- boundary_check_payload (해당 페어인 경우)

부하 절감 효과:
- v6: 전체 페어 50M개 모두에 atoms 산출 → 부담
- v7: 노출 후보 일 90K개에만 atoms 산출 → 부하 90% 이상 감소

### 4.2 호환 점수 계산 (v5 그대로)

호환 점수 자체 계산 방식은 v5와 동일.

### 4.3 compatibility_assessment_basic 산출 (v7 정의)

전체 페어에 대해 다음 산출:

```json
{
  "compatibility_assessment_basic": {
    "assessment_version": "string",
    "final_score": 0.0,
    "qualitative_label": "결이 잘 맞음" | "타협 가능" | "경계 확인",
    "comparable_domain_count": 0,
    "comparable_domain_weight_sum": 0.0,
    "alignment_by_domain": [
      {
        "domain_id": "string",
        "alignment_level": "strong" | "moderate" | "tension" | "soft_conflict",
        "alignment_score": 0.0,
        "tension_score": 0.0,
        "alignment_summary": "string"
      }
    ],
    "shared_sacred_targets": ["string"],
    "queue_reason": "top_match" | "boundary_check"
  }
}
```

**v6와의 차이**:
- pair_reason_atoms 제거 (explanation_payload로 이동)
- public_alignment_sentence, public_tension_sentence 제거 (explanation_payload로 이동)
- alignment_score, tension_score는 *운영 분석용 메타데이터*로 유지 (매칭 알고리즘과 매트릭스 엔진은 alignment_level만 사용)

### 4.4 explanation_payload 산출 (v7 신설, 최종 큐 후보에만)

최종 큐 후보로 선정된 페어에 대해서만 산출:

```json
{
  "explanation_payload": {
    "viewer_id": "string",
    "candidate_id": "string",
    "alignment_by_domain": [
      {
        "domain_id": "string",
        "public_alignment_sentence": "string",
        "public_tension_sentence": "string",
        "pair_reason_atoms": {
          "shared_principles": [
            { "principle_id": "string", "label": "string" }
          ],
          "different_principles": [
            { "viewer_position": "string", "candidate_position": "string" }
          ],
          "shared_sacred_targets": ["string"],
          "shared_rejection_targets": ["string"],
          "tension_targets": ["string"]
        }
      }
    ],
    "boundary_check_payload": null
  }
}
```

#### 4.4.1 shared_principles 추출 (v6 그대로)

두 사용자가 같은 principle을 weight high/medium으로 가짐.

#### 4.4.2 different_principles 추출 (v6 그대로)

같은 axis의 반대 leans.

#### 4.4.3 shared_sacred_targets 추출 (v6 그대로)

양쪽 모두 stance가 require/support + intensity strong/moderate인 target.

#### 4.4.4 shared_rejection_targets 추출 (v7 신설)

양쪽 모두 stance가 reject/avoid + intensity strong/moderate인 target.

```python
def extract_shared_rejection_targets(viewer_targets, candidate_targets):
    """
    공유된 거부감 추출. v4 충돌매트릭스 §0의 'Sanctity' 원칙 — 사람을 묶는 건
    좋아하는 가치만이 아니라 신성시하는 것과 역겨워하는 것이라는 통찰.
    """
    viewer_rejected = set(
        t.target_id for t in viewer_targets
        if t.stance in ["reject", "avoid"]
        and t.intensity in ["strong", "moderate"]
    )
    candidate_rejected = set(
        t.target_id for t in candidate_targets
        if t.stance in ["reject", "avoid"]
        and t.intensity in ["strong", "moderate"]
    )
    return list(viewer_rejected & candidate_rejected)
```

#### 4.4.5 tension_targets 추출 — stance distance matrix 적용 (v7 신설)

v6의 *stance ≠* 방식이 부정확. *require vs support*는 약한 차이, *require vs reject*는 강한 충돌. v7에서 stance distance matrix 적용.

**stance distance matrix**:

```python
STANCE_DISTANCE = {
    "require":  {"require": 0, "support": 1, "allow": 2, "neutral": 3, "avoid": 4, "reject": 5},
    "support":  {"require": 1, "support": 0, "allow": 1, "neutral": 2, "avoid": 3, "reject": 4},
    "allow":    {"require": 2, "support": 1, "allow": 0, "neutral": 1, "avoid": 2, "reject": 3},
    "neutral":  {"require": 3, "support": 2, "allow": 1, "neutral": 0, "avoid": 1, "reject": 2},
    "avoid":    {"require": 4, "support": 3, "allow": 2, "neutral": 1, "avoid": 0, "reject": 1},
    "reject":   {"require": 5, "support": 4, "allow": 3, "neutral": 2, "avoid": 1, "reject": 0},
}
```

**tension 판정 조건 (v7)**:

```python
def is_tension_target(viewer_stance, candidate_stance, viewer_intensity, candidate_intensity):
    distance = STANCE_DISTANCE[viewer_stance][candidate_stance]
    max_intensity = max_intensity_level(viewer_intensity, candidate_intensity)
    
    return distance >= 3 and max_intensity in ["moderate", "strong"]
```

distance >= 3인 stance 조합:
- require ↔ neutral, avoid, reject
- support ↔ avoid, reject
- allow ↔ reject
- neutral ↔ require, reject
- avoid ↔ require, support
- reject ↔ require, support, allow, neutral

이 조합에서만 tension. 약한 차이는 tension 아님.

#### 4.4.6 public_alignment_sentence / public_tension_sentence 생성 (v6 그대로)

생성 규칙은 v6 §4.3.4-§4.3.5 그대로.

### 4.5 boundary_check_payload 산출 (v7 신설)

qualitative_label이 *경계 확인*인 페어에 대해 추가 산출:

```json
{
  "boundary_check_payload": {
    "domain_id": "string",
    "source": "inferred_dealbreaker" | "explicit_dealbreaker",
    "viewer_boundary": "string",
    "candidate_position": "string",
    "confidence": "high" | "medium" | "low"
  }
}
```

**산출 절차**:

```python
def build_boundary_check_payload(viewer, candidate, assessment):
    """
    경계 확인 페어가 왜 경계 확인인지의 구체 정보 산출.
    """
    
    # 1. 어느 영역의 어떤 dealbreaker가 충돌 후보인지 식별
    for domain in viewer.normalized_profile:
        # explicit_dealbreaker 우선
        for db in domain.explicit_dealbreakers:
            candidate_target = find_target(candidate, db.target_id, domain.domain_id)
            if candidate_target and candidate_target.stance in db.unacceptable_stances:
                return {
                    "domain_id": domain.domain_id,
                    "source": "explicit_dealbreaker",
                    "viewer_boundary": db.user_text or canonical_label(db.target_id, db.unacceptable_stances),
                    "candidate_position": candidate_target.label_korean,
                    "confidence": "high"
                }
        
        # inferred_dealbreaker 검사
        for db in domain.inferred_dealbreakers:
            candidate_target = find_target(candidate, db.target_id, domain.domain_id)
            if candidate_target and candidate_target.stance in db.unacceptable_stances:
                return {
                    "domain_id": domain.domain_id,
                    "source": "inferred_dealbreaker",
                    "viewer_boundary": canonical_label(db.target_id, db.unacceptable_stances),
                    "candidate_position": candidate_target.label_korean,
                    "confidence": db.confidence
                }
    
    return None  # 경계 확인 사유를 찾지 못함 — 매우 드문 케이스
```

### 4.6 alignment_score / tension_score (v7 명시)

v6에서 모호했던 부분 명시: alignment_score, tension_score는 *운영 분석용 메타데이터*. 매칭 알고리즘과 매트릭스 엔진은 alignment_level만 사용.

용도:
- 영역별 alignment_score 분포 추적
- 사용자 피드백과의 상관관계 분석 (어느 alignment_score 구간에서 [관심 있음] 비율 높은가)
- 운영자가 alignment_level 임계값 조정 시 참고

매칭 결과·추천 이유에는 영향 없음. 제거하지 않고 유지하되 *운영 분석용*임을 명시.

---

## 5. 필터 통과 판정

**v5 §5 그대로.** 변경 없음.

---

## 6. 발행 후 재시작 처리

**v5 §6 + v7 추가:** 영역 재시작 시 그 사용자의 *모든 관련 페어 explanation_payload 재계산* 트리거 (전체 페어 재계산은 아님 — 최종 큐 후보 페어에만).

---

## 7. 매칭 풀 진입 자격

**v5 §7 그대로.** 변경 없음.

---

## 8. 추천 이유 생성 (v7 갱신)

### 8.1 추천 이유 생성 흐름 (v7)

```
[필터 통과 후보 목록]
  ↓
[최종 큐 후보 산출]
  - 사용자 매칭 화면 노출 후보
  - 점수순 + boundary_check 슬롯
  ↓
[explanation_payload 산출]
  - pair_reason_atoms
  - public_alignment_sentence / public_tension_sentence
  - boundary_check_payload (해당 페어)
  ↓
[추천 이유 매트릭스 엔진]
  - draft_narrative 결정론적 조립
  ↓
[후편집 평가 함수]
  ↓
[후편집 캐시 확인]
  - viewer_id 포함 hash
  - hit → 캐시 결과 + post-polish validation
  - miss → 프롬프트 C 호출 → post-polish validation → 캐시 저장
  ↓
[post-polish validation]
  - 통과 → matches 테이블 저장
  - 실패 → draft_narrative 폴백 (LLM 결과 폐기)
  ↓
[matches 테이블 저장]
```

### 8.2 매트릭스 엔진 입출력 (v7 갱신)

#### 8.2.1 매트릭스 엔진 입력 (v7)

```json
{
  "viewer_user": {
    "user_id": "string",
    "profile_version": "string",
    "core_identity": { ... },
    "summary_by_domain": { ... },
    "domain_published_status": { ... },
    "domain_skip_reasons": { ... }
  },
  "candidate_user": { ... 동일 ... },
  "compatibility_assessment_basic": { ... §4.3 ... },
  "explanation_payload": { ... §4.4 ... }
}
```

**v6와의 차이**: compatibility_assessment 단일 객체 → compatibility_assessment_basic + explanation_payload 분리.

#### 8.2.2 매트릭스 엔진 출력

v6 §8.2.2 그대로.

#### 8.2.3 candidate_brief 처리 (v6 그대로)

v6 §8.2.3 그대로.

#### 8.2.4 매트릭스 엔진 책임 (v7 갱신)

v6 책임 + 추가:
- boundary_check_payload 활용 (boundary_check 페어인 경우)
- candidate_brief 정렬을 *pair relevance 우선*으로 변경

상세는 매트릭스 엔진 v3 문서.

### 8.3 후편집 평가 함수

v6 §8.3 그대로.

### 8.4 프롬프트 C 후편집 + 캐시 key 방향성 분리 (v7 변경)

**v6**: 캐시 key가 페어 양방향 동일 (`min(viewer, candidate)` 정렬)
**v7**: 캐시 key에 *viewer_id 명시*. A→B와 B→A 캐시 분리.

근거: viewer가 다르면 narrative가 다름:
- candidate_brief는 후보 기준이므로 viewer마다 다름
- boundary_check 페어의 *"당신의 경계"*가 누구의 경계인지 viewer마다 다름
- viewer의 core_identity가 후편집 context로 들어가므로 LLM 출력에 영향

**캐시 key (v7)**:

```python
def compute_polish_cache_key(viewer_id, candidate_id, viewer_profile_version, 
                             candidate_profile_version, assessment_version,
                             template_library_version, polish_prompt_version,
                             draft_narrative):
    """
    A→B와 B→A는 별도 캐시.
    """
    draft_hash = stable_hash(
        draft_narrative.headline,
        draft_narrative.alignment_narrative,
        draft_narrative.tension_narrative
    )
    return stable_hash(
        viewer_id,  # 양방향 분리
        candidate_id,
        viewer_profile_version,
        candidate_profile_version,
        assessment_version,
        template_library_version,
        polish_prompt_version,
        draft_hash
    )
```

캐시 hit 가정 (v7 정정): 30% → **15%** (보수적). 운영 데이터로 보강.

### 8.5 Post-Polish Validation (v7 신설)

후편집 LLM 결과를 검증. LLM이 의미 왜곡, 평가어 추가, 상충 뭉개기, raw quote 재도입할 수 있음.

**검증 항목**:

```python
def validate_polish_output(draft, polished, context):
    """
    후편집 결과 검증. 실패 시 draft 폴백.
    """
    
    # 검사 1: raw quote 없음
    if has_raw_quote(polished):
        return ValidationResult(valid=False, reason="raw_quote_introduced")
    
    # 검사 2: 평가어 없음
    if has_evaluative_word(polished):
        return ValidationResult(valid=False, reason="evaluative_word_introduced")
    
    # 검사 3: tension 누락 안 됨
    if context.tension_count > 0 and not polished.tension_narrative.strip():
        return ValidationResult(valid=False, reason="tension_dropped")
    
    # 검사 4: boundary 표현 유지 (boundary_check 페어)
    if context.queue_reason == "boundary_check":
        if not has_boundary_language(polished):
            return ValidationResult(valid=False, reason="boundary_language_dropped")
    
    # 검사 5: 새 영역명 추가 없음
    draft_domains = extract_domain_names(draft)
    polished_domains = extract_domain_names(polished)
    if polished_domains - draft_domains:
        return ValidationResult(valid=False, reason="new_domain_introduced")
    
    # 검사 6: 새 원칙명 추가 없음
    draft_principles = extract_principle_phrases(draft)
    polished_principles = extract_principle_phrases(polished)
    if polished_principles - draft_principles:
        return ValidationResult(valid=False, reason="new_principle_introduced")
    
    # 검사 7: JSON schema valid (이미 LLM 출력 단계에서 강제됨)
    
    # 검사 8: 길이 ±20% 이내 (AI 프롬프트 v6 §1.5.3 제약)
    if not check_length_constraint(draft, polished, tolerance=0.2):
        return ValidationResult(valid=False, reason="length_out_of_range")
    
    return ValidationResult(valid=True)
```

**검증 실패 시 처리**:

```python
def handle_polish_failure(draft, polished, validation_result):
    """
    후편집 실패 시 draft 폴백.
    """
    log_polish_failure(
        viewer_id, candidate_id, validation_result.reason
    )
    
    # 캐시 저장 안 함 (실패한 polish 결과를 재사용하면 안 됨)
    
    # draft_narrative를 최종 결과로 사용
    return draft
```

검증 실패 비율이 일정 임계값(예: 5%) 초과 시 운영자 알림. 후편집 프롬프트 또는 평가 함수 보강 필요.

### 8.6 needs_review 처리 (v7 변경)

**v6**: needs_review 페어를 사용자에게 임시 fallback narrative로 노출
**v7**: 매칭 풀 충분 여부에 따라 분기

```python
def handle_needs_review(match, user_pool_size):
    """
    needs_review 페어 처리.
    """
    
    if user_pool_size >= 10:
        # 매칭 풀 충분 → needs_review 후보를 사용자 노출에서 제외
        match.recommendation_status = "needs_review_hidden"
        # 운영자 검토 큐 추가, 사용자에게는 보이지 않음
    else:
        # 매칭 풀 부족 → 임시 fallback 노출 허용
        match.recommendation_status = "fallback_shown"
        # alignment_summary 기반 임시 narrative 생성
        # UI에 "검토 중" 표시 안 함 (사용자 불안감 회피)
        # 운영자 검토 우선순위 높임
    
    operator_review_queue.add(match.id, priority="high")
```

매칭 풀 충분 임계값 (1차 가정): 사용자별 통과 후보 ≥ 10명. 운영 데이터로 조정.

### 8.7 최종 추천 이유 데이터 계약 (v6 그대로)

v6 §8.7 그대로.

---

## 9. 매칭 결과 데이터 계약

**v5 §9 + 8.7 변경 반영.**

---

## 10. 시스템 동작 보장

**v5 §10 + v6 추가 + v7 추가 보장.**

추가 보장 (v7):
- 부하 분리: 전체 페어 부하와 explanation 부하 분리. 매칭 풀 확장 시 호환 점수 부하만 비례 증가, explanation 부하는 *노출 후보* 수에 비례
- post-polish validation: LLM이 본 앱 톤·정직성을 훼손하지 못하도록 결정론적 검증
- 캐시 key 방향성 분리: A→B와 B→A의 narrative가 정확히 다르게 캐싱
- stance distance matrix: tension 판정의 정밀도 향상

---

## 11. 비용 추정 (v7)

### 11.1 매칭 자체 비용 (v7 정정)

**v6**: 호환 점수 + atoms 함께 계산 → 페어당 ~10-20ms
**v7**: 분리됨

- **1단계 (전체 페어)** — 호환 점수 + alignment_level만: 페어당 ~5-10ms
- **3단계 (최종 큐 후보만)** — atoms + public sentences: 페어당 ~5-10ms 추가

10K 사용자 안정기:
- 전체 페어 (배치): 50M × 7ms = 350K초 = 매우 큰 부하지만 배치 처리, Edge Function 동시성 활용
- 최종 큐 페어: 일 90K × 8ms = 12분 처리

### 11.2 추천 이유 생성 비용 (v6 그대로 + 캐시 정정)

**캐시 hit 가정 v7 정정**: 30% → 15%

10K 사용자 안정기 추천 이유 LLM 비용:
- v6: 캐시 30% 가정 → ~$600-800/월
- v7: 캐시 15% 가정 → ~$700-900/월

캐시 hit 비율은 운영 데이터로 보강. 같은 viewer가 같은 페어를 여러 번 보는 빈도가 캐시 hit의 핵심.

### 11.3 사용자 규모별 월 비용 (v7 정정)

| 활성 사용자 | 일일 신규 | 자기 분석 LLM | 추천 이유 LLM | Supabase | **합계** |
|---|---|---|---|---|---|
| 100 | 5명 | $30 | $5 | $25 | **~$60/월** |
| 1,000 | 20명 | $120 | $55 | $75 | **~$250/월** |
| 5,000 | 30명 | $180 | $330 | $150 | **~$660/월** |
| 10,000 | 50명 | $300 | $800 | $250 | **~$1,350/월** |
| 50,000 | 100명 | $600 | $4,000 | $500 | **~$5,100/월** |

v6 대비 ~5-10% 증가 (캐시 가정 보수적으로). 그래도 10K 사용자 시 사용자당 월 $0.135.

---

## 12. 검토 요청 사항 (v7)

본 v7 결과물 검토 시 다음 지점에 주목.

**(1) compatibility_assessment_basic / explanation_payload 분리.** §4.1의 분리가 부하·구현 측면에서 적절한지. 1단계와 3단계의 trigger 메커니즘이 명확한지.

**(2) stance distance matrix 적용.** §4.4.5의 distance >= 3 임계값이 적절한지. 운영 단계 보강 필요 부분.

**(3) shared_rejection_targets 효과.** §4.4.4의 공유된 거부감 추출이 추천 이유 *결의 정렬* 표현에 기여하는지.

**(4) boundary_check_payload 활용.** §4.5의 payload가 매트릭스 엔진의 boundary headline/narrative에 충분한 정보를 제공하는지.

**(5) raw quote 차단 (정규화 레이어).** §2.2의 검사가 모든 raw quote 유출 경로를 막는지. n-gram overlap 8자 임계값이 적절한지.

**(6) post-polish validation.** §8.5의 8가지 검사가 LLM 출력의 본 앱 톤 훼손을 충분히 막는지.

**(7) needs_review 처리.** §8.6의 매칭 풀 충분 여부 분기가 적절한지. 임계값 10명이 합리적인지.

**(8) 캐시 key 방향성 분리.** §8.4의 viewer_id 포함이 적절한지. 캐시 hit 15% 가정이 보수적으로 적절한지.

**(9) alignment_score / tension_score 운영 분석용 명시.** §4.6의 처리가 코드 구현 시 혼선을 일으키지 않는지.

**(10) v5 참조 영역 standalone 처리.** §13의 v5 참조 절을 다음 사이클에 v7 standalone으로 재수록. 본 사이클은 그대로.

**(11) 누락된 핵심 요소.**

---

## 13. v6에서 v7로의 주요 변경 사항

### 13.1 데이터 분리 — compatibility_assessment_basic + explanation_payload

v6: 단일 compatibility_assessment에 모든 데이터
v7: 2단계 분리, explanation_payload는 최종 큐 후보에만 산출

**관련 변경**:
- §1.2 전체 흐름에 1·2·3·4단계 명시
- §4.3 compatibility_assessment_basic 정의
- §4.4 explanation_payload 정의 (최종 큐 후보 한정)
- §11 비용 추정 정정 (부하 분리 효과)

### 13.2 stance distance matrix 도입

v6: stance ≠ + intensity 차이만 사용
v7: 6×6 stance distance matrix + distance >= 3 임계값

**관련 변경**:
- §4.4.5 tension_targets 추출 정밀화

### 13.3 shared_rejection_targets 추가

본 앱 정체성 (Sanctity 원칙) 강화. 공유된 거부감도 정렬 신호.

**관련 변경**:
- §4.4.4 신설

### 13.4 boundary_check_payload 신설

v6: queue_reason "boundary_check"만 표시
v7: payload로 *어느 영역의 어떤 dealbreaker가 충돌인지* 구체 정보 산출

**관련 변경**:
- §4.5 신설

### 13.5 alignment_score / tension_score 운영 분석용 명시

v6: 모호한 위치
v7: 매칭 알고리즘과 매트릭스 엔진은 alignment_level만 사용. score는 운영 분석용 메타데이터

**관련 변경**:
- §4.6 명시

### 13.6 raw quote 차단 강화

v6: 정규화 레이어 검사 + 매트릭스 엔진 검사
v7: + n-gram overlap 검사 + 운영자 검토 큐 차단

**관련 변경**:
- §2.2 신설

### 13.7 캐시 key 방향성 분리

v6: 페어 양방향 동일 hash
v7: viewer_id 명시 포함, A→B와 B→A 분리

**관련 변경**:
- §8.4 변경
- §11.2 캐시 hit 가정 정정 (30% → 15%)

### 13.8 post-polish validation 신설

v7: 후편집 LLM 결과를 8가지 검사로 검증

**관련 변경**:
- §8.5 신설

### 13.9 needs_review 처리 변경

v6: 모든 needs_review 페어를 임시 fallback으로 노출
v7: 매칭 풀 충분 여부에 따라 분기 (충분 시 노출 차단)

**관련 변경**:
- §8.6 변경

### 13.10 변경 없는 영역

- §2 정규화 레이어 (v5 참조 + raw quote 차단 추가)
- §3 데드라인 필터링 (v5 참조)
- §5 필터 통과 판정 (v5 참조)
- §6 발행 후 재시작 (v5 참조 + explanation 재계산 추가)
- §7 매칭 풀 자격 (v5 참조)
- §9 매칭 결과 데이터 계약 (v5 참조)
- §10 시스템 동작 보장 (v5 참조 + v6·v7 추가)

다음 사이클: v5 참조 영역을 v7 standalone으로 재수록. 본 사이클에서는 시간 절약을 위해 참조 형태 유지.
